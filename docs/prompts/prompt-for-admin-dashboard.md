- build an admin layout, it should be applied to all pages under "/admin/*". On the left top is the site icon(you can use the `mountain-snow` lucide icon) with site name "Jumping Joy Story". Below the site name is the navigation section, currently with two navigation links: "Dashboard" and "Users". On the right top, there is a user avatar icon. When user clicks the avatar icon, a dropdown menu appears with a "Sign out" option.
- The "/admin/dashboard" page should display a welcome message "Welcome to the admin dashboard!"
- The "/admin/users" page should display a list of users in the system using a simple Shadcn table. Each user item should display the user's email, role. I should be able to click on a user item to view/edit the user's details on a Shadcn UI dialog. In the dialog, I should be able to view the user's email, and Shadcn UI switch to toggle the user's role between "user" and "admin". There should be a "Save" button in the dialog to save the changes.

- the first admin user should be created by running a `wrangler d1 execute` command with a SQL statement to update an existing user in the database to have an admin role. Please document the SQL statement and the command in the README file.
